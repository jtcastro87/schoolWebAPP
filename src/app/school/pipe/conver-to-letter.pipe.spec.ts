import { ConverToLetterPipe } from './conver-to-letter.pipe';

describe('ConverToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
