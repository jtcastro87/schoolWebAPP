import { ChangeToLetterPipe } from './change-to-letter.pipe';

describe('ChangeToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
